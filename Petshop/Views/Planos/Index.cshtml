@model IEnumerable<Petshop.Models.Plano>

@{
    ViewData["Title"] = "Lista de Planos";
}

<style>
    /* ======== ESTILO DA TABELA ======== */
    .table-modern {
        border-collapse: collapse;
        width: 100%;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 6px 18px rgba(0,0,0,0.08);
        background-color: #fff;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

        .table-modern th, .table-modern td {
            padding: 14px 18px;
            text-align: center;
            vertical-align: middle;
            transition: background-color 0.3s, color 0.3s;
        }

        .table-modern th {
            background-color: #ff6b9d;
            color: #fff;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.9rem;
        }

        .table-modern tbody tr:nth-child(even) {
            background-color: #f5f7fa;
        }

        .table-modern tbody tr:nth-child(odd) {
            background-color: #ffffff;
        }

        .table-modern tbody tr:hover {
            background-color: #d1e7ff;
            color: #000;
            font-weight: 500;
        }

    /* ======== BOTÕES ======== */
    .btn-main {
        border-radius: 8px;
        padding: 10px 20px;
        font-weight: 600;
        color: #fff;
        text-decoration: none;
        display: inline-block;
        transition: background-color 0.2s;
    }

    .btn-new {
        background-color: #28a745;
    }

        .btn-new:hover {
            background-color: #218838;
        }

    .btn-info {
        background-color: #0dcaf0;
    }

        .btn-info:hover {
            background-color: #0bb5d8;
        }

    .btn-action {
        min-width: 80px;
        padding: 6px 10px;
        border-radius: 6px;
        font-weight: 600;
        transition: background-color 0.2s;
        border: none;
        cursor: pointer;
    }

    .btn-edit {
        background-color: #0d6efd;
        color: #fff;
    }

        .btn-edit:hover {
            background-color: #0b5ed7;
        }

    .btn-details {
        background-color: #6c757d;
        color: #fff;
    }

        .btn-details:hover {
            background-color: #5c636a;
        }

    .btn-delete {
        background-color: #dc3545;
        color: #fff;
    }

        .btn-delete:hover {
            background-color: #c82333;
        }

    /* Container e card */
    .card-custom {
        border: none;
        border-radius: 12px;
        background-color: #fff;
        padding: 20px;
        box-shadow: 0 6px 18px rgba(0,0,0,0.05);
        transition: box-shadow 0.3s ease;
    }

        .card-custom:hover {
            box-shadow: 0 12px 30px rgba(0,0,0,0.1);
        }

    .top-actions {
        display: flex;
        gap: 10px;
        margin-bottom: 20px;
    }

    h2 {
        font-weight: 700;
        color: #0d6efd;
    }
</style>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>@ViewData["Title"]</h2>
        <div class="top-actions">
            <a asp-action="Create" class="btn-main btn-new">+ Novo Plano</a>
            <a asp-action="ClientesPorMeta" class="btn-main btn-info">📊 Clientes que Bateram a Meta</a>
        </div>
    </div>

    <div class="card card-custom shadow-sm">
        <table class="table-modern">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Preço</th>
                    <th>Descrição</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Nome</td>
                            <td>@item.Preco.ToString("C")</td>
                            <td>@item.Descricao</td>
                            <td>
                                <div class="d-flex justify-content-center gap-1">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn-action btn-edit">Editar</a>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn-action btn-details">Detalhes</a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn-action btn-delete">Excluir</a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted py-4">
                            Nenhum plano cadastrado.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
