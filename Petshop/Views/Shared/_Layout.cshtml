<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Petshop</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #ff6b9d;
            --primary-dark: #e5517d;
            --primary-light: #ffacc5;
            --secondary: #4a90e2;
            --accent: #ffa726;
            --success: #66bb6a;
            --text: #2c3e50;
            --text-light: #7f8c8d;
            --bg: #ffffff;
            --bg-light: #f5f7fa;
            --border: #e1e8ed;
            --shadow: rgba(0, 0, 0, 0.08);
            --shadow-hover: rgba(0, 0, 0, 0.15);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
            line-height: 1.6;
            color: var(--text);
            background-color: var(--bg-light);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        header {
            background: linear-gradient(135deg, #ff6b9d 0%, #e5517d 50%, #d94770 100%);
            box-shadow: 0 4px 20px rgba(255, 107, 157, 0.2);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 70px;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

            .logo:hover {
                transform: scale(1.05) translateY(-2px);
                text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            }

            .logo::before {
                content: "🐾";
                font-size: 32px;
                animation: bounce 2s infinite;
            }

        @@keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-5px);
            }
        }

        .nav-toggle {
            display: none;
            background: none;
            border: none;
            color: white;
            font-size: 28px;
            cursor: pointer;
            padding: 5px;
            transition: transform 0.3s ease;
        }

            .nav-toggle:hover {
                transform: scale(1.1);
            }

        nav ul {
            list-style: none;
            display: flex;
            gap: 5px;
            margin: 0;
        }

        nav li {
            position: relative;
        }

        nav a {
            display: block;
            padding: 12px 20px;
            color: white;
            text-decoration: none;
            font-weight: 500;
            border-radius: 8px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

            nav a::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: rgba(255, 255, 255, 0.2);
                transition: left 0.3s ease;
            }

            nav a:hover::before {
                left: 0;
            }

            nav a:hover {
                background-color: rgba(255, 255, 255, 0.15);
                transform: translateY(-2px);
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
            flex: 1;
        }

        main {
            background: var(--bg);
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 8px 30px var(--shadow);
            min-height: calc(100vh - 300px);
            transition: box-shadow 0.3s ease;
        }

            main:hover {
                box-shadow: 0 12px 40px var(--shadow-hover);
            }

        footer {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            margin-top: 60px;
        }

        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 50px 20px 30px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .footer-section h3 {
            font-size: 20px;
            margin-bottom: 20px;
            color: var(--accent);
            font-weight: 600;
            position: relative;
            padding-bottom: 10px;
        }

            .footer-section h3::after {
                content: '';
                position: absolute;
                left: 0;
                bottom: 0;
                width: 50px;
                height: 3px;
                background: var(--accent);
                border-radius: 2px;
            }

        .footer-section p {
            color: #bdc3c7;
            line-height: 1.8;
        }

        .footer-section ul {
            list-style: none;
        }

            .footer-section ul li {
                margin-bottom: 12px;
                padding-left: 20px;
                position: relative;
            }

                .footer-section ul li::before {
                    content: '▸';
                    position: absolute;
                    left: 0;
                    color: var(--accent);
                }

        .footer-section a {
            color: #bdc3c7;
            text-decoration: none;
            transition: all 0.3s ease;
        }

            .footer-section a:hover {
                color: white;
                padding-left: 5px;
            }

        .footer-bottom {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding: 25px 20px;
            text-align: center;
            color: #95a5a6;
            font-size: 14px;
        }

            .footer-bottom a {
                color: var(--accent);
                text-decoration: none;
                transition: color 0.3s ease;
            }

                .footer-bottom a:hover {
                    color: var(--primary-light);
                }

        /* 👇 aqui os ajustes 👇 */
        @@media (max-width: 768px) {
            .nav-toggle {
                display: block;
            }

            nav {
                position: absolute;
                top: 70px;
                left: 0;
                right: 0;
                background: linear-gradient(135deg, #ff6b9d 0%, #e5517d 100%);
                box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
                max-height: 0;
                overflow: hidden;
                transition: max-height 0.4s ease;
            }

                nav.active {
                    max-height: 500px;
                }

                nav ul {
                    flex-direction: column;
                    padding: 10px 0;
                }

                nav a {
                    padding: 15px 25px;
                    border-radius: 0;
                    border-left: 4px solid transparent;
                }

                    nav a:hover {
                        border-left-color: var(--accent);
                        background-color: rgba(255, 255, 255, 0.1);
                    }

            .footer-container {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 30px;
            }

            .footer-section h3::after {
                left: 50%;
                transform: translateX(-50%);
            }

            .footer-section ul li {
                padding-left: 0;
            }

                .footer-section ul li::before {
                    display: none;
                }

            main {
                padding: 25px;
            }

            .container {
                padding: 30px 15px;
            }
        }

        @@media (max-width: 480px) {
            .logo {
                font-size: 22px;
            }

                .logo::before {
                    font-size: 26px;
                }

            .container {
                padding: 20px 10px;
            }

            main {
                padding: 20px;
                border-radius: 12px;
            }

            .footer-container {
                padding: 40px 20px 20px;
            }
        }

        html {
            scroll-behavior: smooth;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        main {
            animation: fadeIn 0.5s ease;
        }
    </style>
</head>
<body>
    <header>
        <div class="header-container">
            <a class="logo" asp-area="" asp-controller="Home" asp-action="Index">Petshop</a>
            <button class="nav-toggle" onclick="toggleMenu()" aria-label="Toggle navigation">☰</button>
            <nav id="mainNav">
                <ul>
                    <li><a asp-area="" asp-controller="Home" asp-action="Index">Home</a></li>
                    <li><a asp-area="" asp-controller="Animais" asp-action="Index">Animal</a></li>
                    <li><a asp-area="" asp-controller="Clientes" asp-action="Index">Cliente</a></li>
                    <li><a asp-area="" asp-controller="Planos" asp-action="Index">Plano</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main role="main">
        @RenderBody()
    </main>

    <footer>
        <div class="footer-container">
            <div class="footer-section">
                <h3>Sobre o Petshop</h3>
                <p>Cuidando do seu pet com carinho e profissionalismo desde 2025. Oferecemos serviços completos para o bem-estar do seu melhor amigo.</p>
            </div>
            <div class="footer-section">
                <h3>Links Rápidos</h3>
                <ul>
                    <li><a asp-area="" asp-controller="Home" asp-action="Index">Home</a></li>
                    <li><a asp-area="" asp-controller="Animais" asp-action="Index">Animais</a></li>
                    <li><a asp-area="" asp-controller="Clientes" asp-action="Index">Clientes</a></li>
                    <li><a asp-area="" asp-controller="Planos" asp-action="Index">Planos</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Contato</h3>
                <ul>
                    <li>📧 contato@petshop.com</li>
                    <li>📱 (11) 1234-5678</li>
                    <li>📍 Rua dos Pets, 123</li>
                    <li>⏰ Seg-Sex: 8h-18h</li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            &copy; 2025 Petshop - Todos os direitos reservados
        </div>
    </footer>

    <script>
        function toggleMenu() {
            const nav = document.getElementById('mainNav');
            nav.classList.toggle('active');
        }

        document.addEventListener('click', function(event) {
            const nav = document.getElementById('mainNav');
            const toggle = document.querySelector('.nav-toggle');
            if (nav && toggle && !nav.contains(event.target) && !toggle.contains(event.target)) {
                nav.classList.remove('active');
            }
        });

        document.querySelectorAll('nav a').forEach(link => {
            link.addEventListener('click', function() {
                const nav = document.getElementById('mainNav');
                if (nav) nav.classList.remove('active');
            });
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>