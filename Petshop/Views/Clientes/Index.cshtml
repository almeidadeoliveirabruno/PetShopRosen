@model IEnumerable<Petshop.Models.Cliente>

@{
    ViewData["Title"] = "Lista de Clientes";
}

<style>
    /* ======== ESTILO DA TABELA ======== */
    .table-modern {
        border-collapse: collapse;
        width: 100%;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 6px 18px rgba(0,0,0,0.08);
        background-color: #fff;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

        .table-modern th, .table-modern td {
            padding: 14px 18px;
            text-align: center;
            vertical-align: middle;
            transition: background-color 0.3s, color 0.3s;
        }

        /* Cabeçalho com cor rosa do layout */
        .table-modern th {
            background-color: #ff6b9d; /* rosa do layout */
            color: #fff;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.9rem;
        }

        /* Linhas alternadas */
        .table-modern tbody tr:nth-child(even) {
            background-color: #f5f7fa;
        }

        .table-modern tbody tr:nth-child(odd) {
            background-color: #ffffff;
        }

        /* Hover */
        .table-modern tbody tr:hover {
            background-color: #d1e7ff; /* leve azul claro */
            color: #000;
            font-weight: 500;
        }

    /* Botões */
    .btn-action {
        min-width: 80px;
        padding: 6px 10px;
        border-radius: 6px;
        font-weight: 600;
        transition: background-color 0.2s;
        border: none;
        cursor: pointer;
        text-decoration: none;
        color: #fff;
    }

    .btn-new {
        background-color: #28a745;
    }

        .btn-new:hover {
            background-color: #218838;
        }

    .btn-edit {
        background-color: #0d6efd;
    }

        .btn-edit:hover {
            background-color: #0b5ed7;
        }

    .btn-details {
        background-color: #6c757d;
    }

        .btn-details:hover {
            background-color: #5c636a;
        }

    .btn-delete {
        background-color: #dc3545;
    }

        .btn-delete:hover {
            background-color: #c82333;
        }

    /* Container e card */
    .card-custom {
        border: none;
        border-radius: 12px;
        background-color: #fff;
        padding: 20px;
        box-shadow: 0 6px 18px rgba(0,0,0,0.05);
        transition: box-shadow 0.3s ease;
    }

        .card-custom:hover {
            box-shadow: 0 12px 30px rgba(0,0,0,0.1);
        }

    .top-actions {
        display: flex;
        gap: 10px;
        margin-bottom: 20px;
    }

    h2 {
        font-weight: 700;
        color: #0d6efd;
    }
</style>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>@ViewData["Title"]</h2>
        <div class="top-actions">
            <a asp-action="Create" class="btn-action btn-new">+ Novo Cliente</a>
            <a asp-action="Ranking" class="btn-action btn-details">🏆 Top 5 Clientes Com Mais Animais</a>
        </div>
    </div>

    <div class="card card-custom shadow-sm">
        <table class="table-modern">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Email</th>
                    <th>Telefone</th>
                    <th>Endereço</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Nome</td>
                            <td>@item.Email</td>
                            <td>@item.Telefone</td>
                            <td>@item.Endereco</td>
                            <td>
                                <div class="d-flex justify-content-center gap-1">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn-action btn-edit">Editar</a>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn-action btn-details">Detalhes</a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn-action btn-delete">Excluir</a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted py-4">
                            Nenhum cliente cadastrado.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
